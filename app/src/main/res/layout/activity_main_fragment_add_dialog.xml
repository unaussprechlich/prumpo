<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="wrap_content">

    <TextView
            android:layout_width="match_parent"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="16dp"
            android:text="@string/map_frag_add_dialog_title"
            android:textSize="18sp"
            android:layout_height="wrap_content"/>

    <LinearLayout
            android:layout_width="match_parent"
            android:orientation="horizontal"
            android:layout_margin="8dp"
            android:layout_height="wrap_content">

        <Button
                android:id="@+id/map_frag_dialog_add_dc"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:textSize="12sp"
                android:text="@string/map_frag_dialog_add_dc"
                android:layout_weight="0.5"/>

        <Button
                android:layout_width="0dp"
                android:id="@+id/map_frag_dialog_add_insurance"
                android:layout_height="match_parent"
                android:text="@string/map_frag_dialog_add_insurance"
                android:textSize="12sp"
                android:layout_weight="0.5"/>

    </LinearLayout>

    <Button
            android:layout_margin="8dp"
            android:id="@+id/map_frag_dialog_abort"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:text="@string/map_frag_dialog_add_abort"/>

</LinearLayout>